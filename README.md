# home
homepage

## Matrix Placeholder

A responsive dark placeholder page using a canvas-based Matrix-style falling code background with a subtle flicker effect.

Open `index.html` in your browser to preview. For a local server you can run:

```powershell
py -m http.server 8000
```

Controls: Pause/Resume and toggle flicker using the buttons on the page or press Space to pause.
